<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.2.xsd
						http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-4.2.xsd">

	<http auto-config='true' use-expressions="true">
		<intercept-url pattern="/**" access="permitAll" /> 
		<access-denied-handler error-page="/denied.do"/>
		<form-login 
			default-target-url="/"
			authentication-success-handler-ref="loginHandler"
		/>
		<logout />

		<!--
		<intercept-url pattern="/login" access="permitAll" />   
		<intercept-url pattern="/resources/**" access="permitAll" />  
		<intercept-url pattern="/**" access="hasRole('USER')" />  
		<form-login login-page="/login"                    
			default-target-url="/monitering"            
			username-parameter="username"         
			password-parameter="password"          
			authentication-failure-url="/login?error"  
			always-use-default-target='true' /> 
		<logout invalidate-session="true" logout-url="/logout" logout-success-url="/login?logout" />
		-->
		
		<!-- 중복로그인 방지 		
		<session-management invalid-session-url="/login">
    	    <concurrency-control max-sessions="1" expired-url="/login"/>
	    </session-management>
	    -->
		<!-- enable csrf protection -->
		<csrf/>  
	</http> 
 	
 	<!-- DB랑 시큐리티 연동 -->
    <authentication-manager>
        <authentication-provider>
            <jdbc-user-service id="userService" data-source-ref="dataSource"
                users-by-username-query="select M.M_ID as username, M.M_PASSWORD as password, A.A_ENABLED as enabled from MEMBER M, AUTHORITY A where M.M_NO=A.M_NO and M.M_ID=?"            
                authorities-by-username-query="select M.M_ID as username, A.A_AUTH as authority from MEMBER M, AUTHORITY A where M.M_NO=A.M_NO and M.M_ID=?"   
            />
        </authentication-provider>
    </authentication-manager>
    
    <!-- 로그인 후 부가작업 -->
    <beans:bean id='loginHandler' 
    	class='bit.spring4.buzbee.util.LoginHandler'>
    </beans:bean>
</beans:beans>
